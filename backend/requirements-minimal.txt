aiohttp==3.12.13
# Core agent framework
uagents==0.22.5
reportlab==4.4.2

# AI API client (for Claude fallback if needed)
anthropic==0.54.0

# Web framework and API
fastapi==0.115.13
uvicorn==0.34.3
python-multipart==0.0.20

# Basic image processing (for preprocessing only)
Pillow==11.2.1

# HTTP client for calling Modal service
aiohttp==3.12.13
httpx>=0.25.0

# Core data handling
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0

# Configuration and utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
aiofiles>=23.0.0

# Development and monitoring
structlog>=23.0.0
rich>=13.0.0

# Remove these heavy dependencies since Modal handles ML:
# torch, torchvision, scikit-learn, datasets, transformers
# huggingface-hub, scikit-image, opencv-python